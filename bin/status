#!/bin/bash

app=$1

echo "$SPATIAL_HOME/gen/$app/sim.log"
#tail -F spatial.log
#tail -F $SPATIAL_HOME/gen/$app/build.log
#tail -F $SPATIAL_HOME/gen/$app/sim.log
tail -10 $SPATIAL_HOME/gen/$app/sim.log
#tail -F $LOG_FILE
