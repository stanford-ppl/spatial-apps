#!/bin/bash

app=$1
log=$2

cd $PIR_HOME
mkdir -p $PIR_HOME/out/$app
out_dir=$PIR_HOME/out/$app
$PIR_HOME/bin/pir $app --debug --mapping=true --verbose --simulate=false --arch=SN16x8_LD >> $log 2>&1

echo "-------------PASS (DONE)------------" >> $log
